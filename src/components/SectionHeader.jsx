import React from 'react'

const SectionHeader = ({text}) => {
  return (
    <div className="header-text">
      <h2 className="title-tx text">{text}</h2>
    </div>
  )
}

export default SectionHeader;
